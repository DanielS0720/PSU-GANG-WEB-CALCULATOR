# @cloudflare/next-on-pages peer-declares next <=15.5.2; this project runs a
# newer patch (15.5.19). The mismatch is patch-level and works in practice, so
# allow installs (local + Cloudflare Pages CI) to proceed.
legacy-peer-deps=true
